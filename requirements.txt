# Packages Python requis
musdb>=0.4.0
librosa>=0.10.0
numpy>=1.21.0
soundfile>=0.12.0
matplotlib>=3.5.0
museval>=0.4.0  # Pour l'évaluation des métriques SDR, SIR, SAR
pandas>=1.3.0  # Pour le traitement des résultats d'évaluation

# Framework d'apprentissage profond
# Note : PyTorch doit être installé séparément (voir explication ci-dessous)
# torch>=1.12.0  # Installez d'abord PyTorch : pip install torch torchvision torchaudio
tensorboard>=2.10.0
tqdm>=4.64.0

# Note : musdb nécessite la dépendance système ffmpeg
# Installation dans Ubuntu/WSL : sudo apt-get install -y ffmpeg
# Vérification : ffmpeg -version

