# Python依赖包
musdb>=0.4.0
librosa>=0.10.0
numpy>=1.21.0
soundfile>=0.12.0
matplotlib>=3.5.0

# 深度学习框架
# 注意：PyTorch需要单独安装（见下面说明）
# torch>=1.12.0  # 请先安装PyTorch: pip install torch torchvision torchaudio
tensorboard>=2.10.0
tqdm>=4.64.0

# 注意：musdb需要系统依赖ffmpeg
# 在Ubuntu/WSL中安装: sudo apt-get install -y ffmpeg
# 验证: ffmpeg -version

